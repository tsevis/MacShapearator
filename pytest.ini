[pytest]
# The Swift suite lives in Tests/MacShapearatorTests and is run by swift test;
# these are the Python bridge contract tests, which need a Shapearator
# checkout (SHAPEARATOR_SRC, ./engine, or ../shapearator).
testpaths = Tests/bridge
